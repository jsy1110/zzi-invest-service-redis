spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

  profiles:
    default: local

server:
  servlet:
    encoding:
      charset: UTF-8
      force: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


